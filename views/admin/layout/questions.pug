extends master.pug
block topvar
  - topActive = "Modules"
  - subPages = [{name: "Browse", href:"/admin/modules/" + modid}, {name: "Create New", href: "/admin/modules/" + modid + "/new"}]
  block subvar
block inner
  .container
    +navbar("Questions for " + modid, "modules", subPages, subActive)
    block contents